---
interface Props {
	date?: Date; // <-- ora opzionale
}

const { date } = Astro.props;
---

{date ? (
	<time datetime={date.toISOString()}>
		{
			date.toLocaleDateString('en-us', {
				year: 'numeric',
				month: 'short',
				day: 'numeric',
			})
		}
	</time>
) : null}
